// import { NextApiRequest, NextApiResponse } from "next";
// import PrismaClient from "../connection";
// import multer from "multer";

// export default async function UPDATE(request: NextApiRequest, response: NextApiResponse) {
//     let account = await PrismaClient.account.findFirst({where: {id: Number(request.query.id)}});
//     let upload = multer({ dest: "./public/profiles/images" });
//     upload.single()
// }

